<?php

declare(strict_types=1);

namespace <NAMESPACE>\<CONTEXT>\Application\Command<CONTAINER>;

use Assert\AssertionFailedException;
use <NAMESPACE>\Shared\Application\Command\CommandHandler;
use <NAMESPACE>\<CONTEXT>\Application\Command<CONTAINER>\<NAME>Command;

final class <NAME>CommandHandler implements CommandHandler
{
    public function __construct()
    {
    }

    /** @throws AssertionFailedException */
    public function __invoke(ResendUserInviteCommand $command): void
    {
        echo 'handler';
        exit;

        /* $this->eventBus->publish(...$userAccess->events()); */
    }
}
